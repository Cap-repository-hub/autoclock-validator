[all:vars]
ansible_user=ubuntu

[validators]
# ansible_host should be the IP of your validator or run ansible local on setup node
# validator ansible_host=10.0.0.2 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa
validator ansible_host=localhost ansible_user=ubuntu ansible_connection=local
